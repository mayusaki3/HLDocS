目次 > フィードバック > HTML生成PoC実行結果記録テンプレート

# HTML生成PoC実行結果記録テンプレート

## 1. 位置付け

本書は、HLDocS HTML生成PoCの実行結果を記録するためのテンプレートである。

本書は正規仕様ではなく、PoC検証結果を仕様へ一括反映する前段の記録用フィードバック文書として扱う。

---

## 2. 実行情報

### 2.1 実行日時

```text
未記録
```

### 2.2 実行ブランチ

```text
feature/html-visualization-spec
```

### 2.3 実行commit

```text
未記録
```

### 2.4 実行方法

該当するものを記録する。

```text
- GitHub Actions
- local
- other
```

### 2.5 実行コマンド

```bash
python tools/html_generator/generate_html.py \
  --input docs/ja-JP \
  --output docs/ja-JP/HTMLドキュメント \
  --profile overview,reference
```

---

## 3. pytest結果

### 3.1 実行コマンド

```bash
python -m pytest tools/html_generator
```

### 3.2 結果

```text
未記録
```

### 3.3 失敗テスト

```text
なし / 未記録
```

### 3.4 修正要否

```text
未記録
```

---

## 4. 生成物確認

### 4.1 index.html

確認対象：

```text
docs/ja-JP/HTMLドキュメント/index.html
```

結果：

```text
未記録
```

---

### 4.2 overview/index.html

確認対象：

```text
docs/ja-JP/HTMLドキュメント/overview/index.html
```

結果：

```text
未記録
```

---

### 4.3 reference HTML

確認対象：

```text
docs/ja-JP/HTMLドキュメント/reference/*.html
```

結果：

```text
未記録
```

---

### 4.4 site-manifest.json

確認対象：

```text
docs/ja-JP/HTMLドキュメント/manifest/site-manifest.json
```

結果：

```text
未記録
```

---

## 5. Manifest確認

### 5.1 必須項目

確認項目：

- page_id
- profile
- source_markdown
- canonical_title
- doc_id
- sec_id
- output_html_path
- source_hash
- stale
- link_targets

結果：

```text
未記録
```

---

### 5.2 link_targets

確認内容：

- Manifest に存在しない page_id への link_targets がないこと

結果：

```text
未記録
```

---

### 5.3 stale 初期状態

確認内容：

- 初回生成時の stale が false であること
- source_hash が記録されていること

結果：

```text
未記録
```

---

## 6. 表示確認

### 6.1 overview表示

確認内容：

- HLDocS の目的が表示されること
- generated / canonical の関係が表示されること
- not generated 対象が表示されること

結果：

```text
未記録
```

---

### 6.2 reference表示

確認内容：

- canonical_title が表示されること
- doc_id が表示されること
- document_type が表示されること
- Markdown 正本リンクが表示されること

結果：

```text
未記録
```

---

### 6.3 表示名リンク

確認内容：

- 人間向けリンク表示名に物理ファイル名を表示していないこと
- 人間向けリンク表示名に並び順制御番号を表示していないこと

結果：

```text
未記録
```

---

## 7. ファイル名確認

### 7.1 空白入りファイル名

確認コマンド：

```bash
find docs/ja-JP/HTMLドキュメント -name "* *"
```

期待結果：

```text
出力なし
```

結果：

```text
未記録
```

---

## 8. 対象外確認

### 8.1 test-report

確認内容：

- test-report が推測生成されていないこと
- 必要に応じて not generated と表示されていること

結果：

```text
未記録
```

---

### 8.2 traceability

確認内容：

- 完全な Traceability 可視化が推測生成されていないこと
- 必要に応じて not generated と表示されていること

結果：

```text
未記録
```

---

## 9. 仕様反映候補

PoC結果から仕様へ反映すべき内容を記録する。

```text
未記録
```

---

## 10. 修正候補

PoC結果から generator / tests / CI へ反映すべき内容を記録する。

```text
未記録
```

---

## 11. 判定

### 11.1 PoC-1完了候補

```text
PASS / FAIL / 未判定
```

### 11.2 理由

```text
未記録
```

---

目次 > フィードバック > HTML生成PoC実行結果記録テンプレート
